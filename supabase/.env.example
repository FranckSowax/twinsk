# Secrets des fonctions Edge Supabase (`supabase secrets set --env-file …`).
#
# Aucun à ce jour : le projet n'a pas de fonction Edge. Toute la logique
# serveur tourne dans les routes API Next.js (Railway) ; leurs variables sont
# décrites dans /.env.example et dans docs/multi-country/03-server-secrets.md.
#
# Si une fonction Edge est ajoutée, déclarer ici ses secrets (noms seulement),
# dont COUNTRY=GA|CI pour qu'elle lise le pays avec Deno.env.get('COUNTRY').
